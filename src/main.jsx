import React from 'react'
import ReactDOM from 'react-dom/client'
import WardManager from './WardManager.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WardManager />
  </React.StrictMode>
)
